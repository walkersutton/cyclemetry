{
  "name": "cyclemetry-app-react",
  "nhahhomepage": "localhost",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@json-editor/json-editor": "^2.15.1",
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/react": "^15.0.7",
    "@testing-library/user-event": "^14.5.2",
    "axios": "^1.7.2",
    "bootstrap": "^5.3.3",
    "gh-pages": "^6.1.1",
    "lodash": "^4.17.21",
    "npm-check": "^6.0.1",
    "npm-run-all": "^4.1.5",
    "react": "^18.3.1",
    "react-bootstrap": "^2.10.4",
    "react-dom": "^18.3.1",
    "react-resizable-panels": "^2.0.22",
    "react-scripts": "5.0.1",
    "web-vitals": "^4.0.1"
  },
  "scripts": {
    "uhhpredeploy": "npm run build",
    "uhhdeploy": "gh-pages -d build",
    "uhhstart": "react-scripts start",
    "uhhbuild": "react-scripts build",
    "uhhtest": "react-scripts test",
    "uhheject": "react-scripts eject",
    "start:eel": "python3 app.py --develop",
    "start:js": "react-scripts start",
    "start": "npm run start:eel & npm run start:js",
    "build:js": "react-scripts build",
    "ddbuild:eel": "python3 -m eel app.py build --add-data=public:public",
    "build:eel": "python3 -m eel app.py build",
    "devbuild:js": "react-scripts build",
    "devbuild:eel": "python3 -m eel app.py build --onefile",
    "clean:build": "rm -rf build dist",
    "build": "npm-run-all clean:build build:*",
    "devbuild": "npm-run-all clean:build devbuild:*",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "sass": "^1.77.3"
  }
}
